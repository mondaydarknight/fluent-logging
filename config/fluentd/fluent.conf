<source>
  # @type forward
  # path /var/log/nginx/tracking.log
  # pos_file /var/log/fluentd/httpd-access.log.pos
  # port 24224
  # tag nginx.access.tracking
  # <parse>
  #   @type json
  #   time_format %Y-%m-%dT%H:%M:%S.%NZ
  # </parse>
  @type tail
  format none
  path /var/log/nginx/tracking.log
  pos_file /var/log/fluentd/httpd-access.log.pos
  tag nginx.access.tracking
</source>
# <filter nginx.access.tracking>
#   @type parser
#   <parse>
#     @type json
#     json_parser json
#   </parse>
#   replace_invalid_sequence true
#   emit_invalid_record_to_error false
#   key_name log
#   reserve_data true
# </filter>
<match **>
  @type stdout
  # @type copy
  # <store>
  #   @type mongo
  #   database crm
  #   collection EventStream
  #   host mongo
  #   port 27017
  # </store>
</match>
